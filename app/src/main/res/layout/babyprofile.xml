<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/babycontent"
    android:layout_width="120dp"
    android:layout_height="120dp"
    android:layout_gravity="center"
    android:orientation="vertical">
    <!--<ImageView
    android:layout_width="100dp" android:scaleType="fitCenter"
    android:layout_height="80dp"

    android:background="@drawable/circle" android:id="@+id/babyimg"
    android:src="@drawable/logocaletapp"/>-->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/babyimg"
        android:layout_width="120dp"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:src="@drawable/baby"
        app:civ_border_color="@color/caletagrey"
        app:civ_border_width="2dp" />

    <TextView
        android:id="@+id/babynameHeader"
        android:layout_width="120dp"
        android:layout_height="20dp"
        android:layout_gravity="center"
        android:gravity="center" android:textSize="16sp"
        android:text=""
        android:textColor="@color/caletagrey"
        android:textStyle="bold"></TextView>

</LinearLayout>